# Deploy — AWS Cloud  (moved)

> **This document has moved.** The authoritative, end-to-end, battle-tested
> procedure now lives in **[DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)**.
>
> - Cloud deployment from scratch → **Part A**
> - Add a store → **Part B**
> - Updating → **Part D**
> - Troubleshooting → **Part E**
>
> This older topic doc was removed because it predated the production fixes
> (Postgres `schema.sql` seeding, Alembic auth, Redis TLS `ssl_cert_reqs`, real
> S3 endpoint + bucket CORS, lean single-node profile, nip.io hostnames) and
> would otherwise reproduce already-solved issues. Use DEPLOYMENT_GUIDE.md.
