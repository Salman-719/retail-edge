# Offline ML-lifecycle (eval) dependencies ONLY.
# Intentionally separate from services/*/requirements.txt so production images
# stay lean and can never import mlflow.
#
# Versions PINNED to the verified host env. boxmot is pinned hard: the harness and
# services/iep2_vision/tracker/tracker.py use the boxmot 10.0.x API (BoTSORT,
# ByteTrack, OcSort, StrongSort classes). boxmot 19.x renamed/removed these and
# breaks the import — never let pip resolve "latest".

mlflow==2.16.2
psutil>=5.9            # MLflow system metrics (CPU/RAM) — also pulled by ultralytics
boto3>=1.34
numpy==1.26.4
pandas>=2.0
matplotlib>=3.8
opencv-python==4.11.0.86
ultralytics==8.3.230    # RT-DETR / YOLO detectors for the detection experiment
boxmot==10.0.84         # BoT-SORT / ByteTrack / OC-SORT / StrongSORT (matches services/iep2_vision)
