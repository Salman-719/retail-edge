VITE_API_URL=http://localhost:8000
VITE_LIVE_BRIDGE_URL=ws://localhost:8010
VITE_CAMERAS=cam-01,cam-02

# Dev only — IEP2 Vision Debug Console (/dev/vision).
# Start the dev server:  docker compose --profile dev up -d iep2_dev
# Remove this var when VisionDebugConsole.jsx is deleted before ship.
VITE_IEP2_DEV_API_URL=http://localhost:8002
